<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Green style | Make your life green</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body onLoad='notifyAll("<%= notice %>")'>
    <div class="menu__bar block--fixed block--height-large block--width-full">
      <div class="menu__container">
        <div class="logo_container block--height-full">
            <p class="font--size-xlarge line--height-large"><span class="logo logo--color-green">Green</span> style</p>
        </div>
        <div class="menu block--height-full">
          <div class="menu__item">
            <p>Trang chủ</p>
          </div>
          <div class="menu__item">
            <p>Bàn phím cơ</p>
          </div>
          <div class="menu__item">
            <p>Chuột</p>
          </div>
          <div class="menu__item">
            <p>Key cap</p>
          </div>
          <div class="menu__item">
            <p>Tai nghe</p>
          </div>
          <div class="menu__item">
            <p>Loa</p>
          </div>
        </div>
        <div class="menu__user__container block--relative">
          <div class="menu__user__login line--height-large">
            <% if !current_user %>
              <%= link_to 'Đăng nhập', new_user_session_path %>
            <% else %>
              <%= link_to 'Đăng xuất', destroy_user_session_path, method: 'delete' %>
              <%= current_user.name %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <%# <%= yield %>
    <%# %>
    <div>
      <div style="width:20%; float: left; margin-top: 100px">
        <%= link_to 'Tất cả sản phẩm', products_path %> <br/>
        <% Category.where(category_id: nil).all.each do |cate| %>
          <%= link_to "#{cate.name}", show_category_product_path(cate.id) %> <br/>
          <% cate.categories.each do |sub_cate| %>
            - <%= link_to "#{sub_cate.name}", show_category_product_path(sub_cate.id) %> <br/>
          <% end %> <!-- end sub_cate -->
        <% end %> <!-- end cate -->
      </div> <!-- div for menu -->
      <div style="width:60%; float: left">
        <%= yield %>
      </div> <!-- div for frame -->
    </div>
  </body>
</html>
