<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Green style | Make your life green</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body onLoad='notifyAll("<%= notice %>")'>
    <div style="width:100%">
      <% if !current_user %>
        <%= link_to 'Đăng nhập', new_user_session_path %>
      <% else %>
        <%= link_to 'Đăng xuất', destroy_user_session_path, method: 'delete' %>
        <%= link_to "#{current_user.name}", user_path(current_user.id) %>
      <% end %>
    </div>
    <h1>Green Style</h1>
    <p>Trải nghiệm xanh, cuộc sống xanh</p>
    
    <div>
      <div style="width:20%; float: left">
        <% Category.where(category_id: nil).all.each do |cate| %>
          <%= link_to "#{cate.name}", products_path %> <br/>
          <% cate.categories.each do |sub_cate| %>
            - <%= link_to "#{sub_cate.name}", products_path %> <br/>
          <% end %> <!-- end sub_cate -->
        <% end %> <!-- end cate -->
      </div> <!-- div for menu -->
      <%= link_to "Thêm sản phẩm", new_product_path %>
      <div style="width:80%">
        <%= yield %>
      <div> <!-- div for frame -->
    </div>
  </body>
</html>
