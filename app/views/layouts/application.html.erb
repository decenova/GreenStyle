<!DOCTYPE html>
<html>
  <head>
    <title>GreenStyle</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body onLoad='notifyAll("<%= notice %>")'>
    <div style="width:100%">
      <% if !current_user %>
        <%= link_to 'Đăng nhập', new_user_session_path %>
      <% else %>
        <%= link_to 'Đăng xuất', destroy_user_session_path, method: 'delete' %>
        <%= current_user.name %>
      <% end %>
    </div>
    <h1>Green Style</h1>
    <p>Trải nghiệm xanh, cuộc sống xanh</p>
    <div>
      <div style="width:20%; float: left">
        <% Category.all.each do |cate| %>
          <%= link_to "#{cate.name}", "#" %> <br/>
          <% cate.categories.each do |sub_cate| %>
            - <%= link_to "#{sub_cate.name}", "#" %> <br/>
        <% end %>
      <% end %>
      </div>
      <div style="width:80%">
        <%= yield %>
      <div>
    </div>
  </body>
</html>
